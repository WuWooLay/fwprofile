{
  "name": "bootstrap-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "serve",
    "dev": "concurrently \"live-server --port=4500\" \"node-sass ./assets/sass/ -w -o ./assets/css/ --output-style compressed \"",
    "build": "node-sass ./assets/sass/ -o ./assets/css/ --output-style compressed "
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "live-server": "^1.2.1",
    "node-sass": "^4.13.1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serve": "^11.0.0"
  }
}
